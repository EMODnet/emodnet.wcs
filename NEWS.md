# emodnet.wcs (development version)

* Bug fix: Fixed `coverage_id` scoping issue in `emdn_get_coverage()` success message (#118).

# emodnet.wcs 0.0.1

* First release, initial version.

* Added a `NEWS.md` file to track changes to the package.
